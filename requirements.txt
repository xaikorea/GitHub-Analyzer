pocketflow>=0.0.1
pyyaml>=6.0
requests>=2.28.0
gitpython>=3.1.0
google-cloud-aiplatform>=1.25.0
google-genai>=1.9.0
python-dotenv>=1.0.0
pathspec>=0.11.0

streamlit>=1.37.0

psycopg[binary]>=3.2.0
beautifulsoup4>=4.12.0
markdown-it-py>=3.0.0

# LoRA fine-tuning dependencies (torch, etc.) are heavy and only needed for
# train_lora_local.py / infer_lora_local.py. Install them separately with:
#   pip install -r requirements-train.txt
